- add ~/.cdent/config.yaml and ./.cdent/config.yaml support (CDENT_CONFIG)

+ --emit-header
+ --emit-trailer
+ --emit-info
+ env variable for all command defaults

+ Add Copyright to generator
+ Add LICENSE file
+ Get cdent --version working
